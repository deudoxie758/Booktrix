import { WorkspacePlaceholder } from '@/components/business/WorkspacePlaceholder'; import { requireWorkspaceRole } from '@/modules/organizations/context'; export default async function Page(){await requireWorkspaceRole(['OWNER','ACCOUNTS']);return <WorkspacePlaceholder title="Finance" description="Revenue, cash reconciliation, commissions, refunds, subscriptions, and exports arrive in the payments and finance phase."/>}
