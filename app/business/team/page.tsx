import { WorkspacePlaceholder } from '@/components/business/WorkspacePlaceholder'; import { requireWorkspaceRole } from '@/modules/organizations/context'; export default async function Page(){await requireWorkspaceRole(['OWNER','MANAGER']);return <WorkspacePlaceholder title="Team" description="Owners and managers will invite staff, assign roles, and control location access here."/>}
