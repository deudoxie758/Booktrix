import { WorkspacePlaceholder } from '@/components/business/WorkspacePlaceholder'; import { requireWorkspaceRole } from '@/modules/organizations/context'; export default async function Page(){await requireWorkspaceRole(['OWNER']);return <WorkspacePlaceholder title="Business settings" description="Owners will manage company profile, subscription, policies, and integrations here."/>}
