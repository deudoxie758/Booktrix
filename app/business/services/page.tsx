import { WorkspacePlaceholder } from '@/components/business/WorkspacePlaceholder'; import { requireWorkspaceRole } from '@/modules/organizations/context'; export default async function Page(){await requireWorkspaceRole(['OWNER','MANAGER']);return <WorkspacePlaceholder title="Services" description="Configure prices, durations, capacity, staff eligibility, and payment policies here."/>}
