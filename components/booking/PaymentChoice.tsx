export function PaymentChoice({ choices, value, onChange }: { choices: readonly string[]; value?: string; onChange: (choice: string) => void }) {
  const labels: Record<string, string> = { FULL: 'Pay in full online', DEPOSIT: 'Pay a deposit online', CASH: 'Pay cash at the appointment' }
  return <fieldset className="space-y-3"><legend className="font-display text-2xl text-cocoa-950">How would you like to pay?</legend>{choices.map((choice) => <label key={choice} className="flex min-h-14 items-center gap-3 rounded-2xl border border-sand-200 bg-white px-4"><input type="radio" name="paymentChoice" value={choice} checked={value === choice} onChange={() => onChange(choice)} className="h-5 w-5 accent-clay-600" /><span className="font-semibold text-cocoa-800">{labels[choice] ?? choice}</span></label>)}</fieldset>
}
