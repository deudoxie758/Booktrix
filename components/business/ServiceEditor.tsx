type LocationOption = { id: string; name: string }
export function ServiceEditor({ locations, action }: { locations: LocationOption[]; action?: (formData: FormData) => void | Promise<void> }) {
  const input = 'mt-1 w-full rounded-xl border border-sand-300 bg-white px-3 py-2'
  return <form action={action} className="space-y-5 rounded-3xl border border-sand-200 bg-white p-5 shadow-sm sm:p-6"><h2 className="font-display text-2xl text-cocoa-950">Add a service</h2><div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold">Service name<input required name="name" className={input} /></label><label className="text-sm font-semibold">Category<input required name="category" className={input} /></label><label className="text-sm font-semibold">Duration (minutes)<input required min="1" name="durationMinutes" type="number" className={input} /></label><label className="text-sm font-semibold">Price (EC$)<input required min="0" step="0.01" name="price" type="number" className={input} /></label><label className="text-sm font-semibold">Preparation buffer<input min="0" defaultValue="0" name="preparationMinutes" type="number" className={input} /></label><label className="text-sm font-semibold">Cleanup buffer<input min="0" defaultValue="0" name="cleanupMinutes" type="number" className={input} /></label><label className="text-sm font-semibold">Capacity<input required min="1" defaultValue="1" name="capacity" type="number" className={input} /></label><label className="text-sm font-semibold">Confirmation<select aria-label="Confirmation" name="confirmationMode" className={input}><option value="AUTOMATIC">Automatic</option><option value="MANUAL">Manual approval</option></select></label></div><fieldset><legend className="text-sm font-semibold">Available locations</legend><div className="mt-2 flex flex-wrap gap-4">{locations.map((location) => <label key={location.id}><input type="checkbox" name="locationIds" value={location.id} /> {location.name}</label>)}</div></fieldset><fieldset><legend className="text-sm font-semibold">Payment options</legend><div className="mt-2 flex flex-wrap gap-4"><label><input type="checkbox" name="allowFullPayment" defaultChecked /> Full payment</label><label><input type="checkbox" name="allowDeposit" /> Deposit</label><label><input type="checkbox" name="allowCash" defaultChecked /> Cash</label></div><div className="mt-3 grid gap-3 sm:grid-cols-2"><label className="text-sm">Deposit type<select name="depositKind" className={input}><option value="">Not configured</option><option value="FIXED">Fixed EC$</option><option value="PERCENTAGE">Percentage</option></select></label><label className="text-sm">Deposit value<input min="0" name="depositValue" type="number" className={input} /></label></div></fieldset><button className="rounded-full bg-cocoa-900 px-5 py-3 text-sm font-semibold text-white">Save service</button></form>
}
